# AI Companion Web - Environment Variables Example
# Copy this file to .env and fill in your actual values

# =============================================================================
# DATABASE CONFIGURATION
# =============================================================================

# Full database connection URL (optional if using individual components below)
DATABASE_URL=postgresql://username:password@localhost:5432/ai_companion_db

# Individual database connection components
DATABASE_PW=your_database_password_here
DATABASE_USER=your_database_username
DATABASE_NAME=ai_companion_db
DATABASE_HOST=localhost
DATABASE_PORT=5432

# =============================================================================
# SECURITY CONFIGURATION
# =============================================================================

# Secret key for JWT token signing (generate a strong random key)
SECRET_KEY=your_super_secret_key_here_make_it_at_least_32_characters_long

# =============================================================================
# SERVER CONFIGURATION
# =============================================================================

# Server host and port
HOST=localhost
PORT=7777

# Local URLs for development
LOCAL_URL_SSL=https://localhost:7777
LOCAL_URL=http://localhost:7777
LOCAL_IP=127.0.0.1

# =============================================================================
# CORS CONFIGURATION
# =============================================================================

# Allowed origins for CORS (comma-separated for multiple origins)
CORS_ORIGINS=https://localhost:7777,http://localhost:7777

# =============================================================================
# JWT TOKEN CONFIGURATION
# =============================================================================

# Token expiration times (in minutes for access, days for refresh)
ACCESS_TOKEN_EXPIRE_MINUTES=30
REFRESH_TOKEN_EXPIRE_DAYS=3

# =============================================================================
# NOTES
# =============================================================================
# 
# 1. Never commit your actual .env file to version control
# 2. Generate a strong SECRET_KEY using: python -c "import secrets; print(secrets.token_urlsafe(32))"
# 3. Update CORS_ORIGINS for production with your actual domain
# 4. Use environment-specific database credentials for production
# 5. Consider using a secrets manager for production deployments
